---
- hosts: all
  become: true
  tasks:

    - name: Gather service facts
      service_facts:
    
    - name: Restart containerd service
      systemd:
        name: containerd
        state: restarted
      when: "'containerd' in ansible_facts['services']"
